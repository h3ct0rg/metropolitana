import { InterpolateValuesPipe } from './interpolate-values.pipe';

describe('InterpolateValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new InterpolateValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
