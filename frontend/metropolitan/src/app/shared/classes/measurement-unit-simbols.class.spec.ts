import { MeasurementUnitSimbols.Class } from './measurement-unit-simbols.class';

describe('MeasurementUnitSimbols.Class', () => {
  it('should create an instance', () => {
    expect(new MeasurementUnitSimbols.Class()).toBeTruthy();
  });
});
